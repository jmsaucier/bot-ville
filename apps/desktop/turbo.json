{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "package": {
      "dependsOn": ["build"],
      "outputs": ["release/**"]
    }
  }
}
